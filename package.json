{
  "name": "foco",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "backend": "npm run build -w web && node scripts/dev-backend.mjs",
    "frontend": "npm run dev -w web",
    "test": "cargo test --workspace && npm run test -w web && npm run typecheck -w web",
    "test:release-smoke:windows": "node scripts/smoke-release-windows.mjs",
    "typecheck": "npm run typecheck -w web",
    "build:release": "npm run build -w web && cargo build --release -p foco-app"
  }
}
